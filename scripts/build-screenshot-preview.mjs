import { readFileSync, writeFileSync } from 'node:fs';
const escape = value => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const embed = path => `data:image/${path.endsWith('.png') ? 'png' : 'jpeg'};base64,${readFileSync(path).toString('base64')}`;
const projects = JSON.parse(readFileSync('assets/data/projects.json', 'utf8'));
const figure = (path, caption) => `<figure><img src="${embed(path)}" alt="${escape(caption)}"><figcaption>${escape(caption)}</figcaption></figure>`;
const panels = ['before','after'].map(name => figure(`docs/verification/compact-panels/${name}.png`, name === 'before' ? 'Before: three tall panels' : 'After: tools and contact stacked beside experience')).join('');
const sections = projects.map(project => `<section id="${escape(project.name.toLowerCase().replaceAll(' ','-'))}"><h2>${escape(project.name)}</h2><p>${escape(project.description)}</p><div class="gallery">${project.gallery.map(shot => figure(`public${shot.src}`, shot.caption || project.name)).join('')}</div></section>`).join('');
writeFileSync('docs/project-screenshots.html', `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Portfolio screenshot showcase</title><style>*{box-sizing:border-box}body{margin:0;background:#171817;color:#f7f7f3;font:16px/1.6 system-ui,sans-serif}main{max-width:1400px;margin:auto;padding:32px 24px}h1,h2{line-height:1.15;letter-spacing:-.04em}h1{font-size:36px}h2{font-size:28px}p,figcaption{color:#c5c9c1}nav{display:flex;flex-wrap:wrap;gap:20px;margin:24px 0}a{color:#acfa58}section{padding:24px 0;border-top:1px solid #41443c}.gallery,.comparison{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px}figure{margin:0}img{display:block;width:100%;height:auto;border:1px solid #41443c;border-radius:10px}figcaption{font-size:13px;margin:8px 0 20px}section>p{max-width:85ch}@media(max-width:760px){.gallery,.comparison{grid-template-columns:1fr}main{padding:20px 16px}}@media print{body{background:white;color:black}section{break-inside:avoid}p,figcaption{color:#333}}</style></head><body><main><h1>Portfolio screenshot showcase</h1><p>Project screenshots in showcase order, with the compact panel update. All images are embedded in this file and work offline.</p><nav><a href="#panels">Panel comparison</a>${projects.map(p => `<a href="#${escape(p.name.toLowerCase().replaceAll(' ','-'))}">${escape(p.name)}</a>`).join('')}</nav><section id="panels"><h2>A more compact layout</h2><div class="comparison">${panels}</div></section>${sections}<p>Arawan captures use synthetic browser responses; no dummy loan or borrower records were saved. Sentrix and Forkcast images are the existing project captures.</p></main></body></html>`);
